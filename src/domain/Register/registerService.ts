import {IUserAPI} from '@domain';

const registerUser = async (data: IUserAPI) => {};

const registerRepository = async (data: IRepositoryAPI) => {};

export const RegisterService = {
  registerUser,
  registerRepository,
};
